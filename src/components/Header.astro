---
const currentPath = Astro.url.pathname;

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/blog/', label: 'Blog' },
  { href: '/knowledge_base/', label: 'Knowledge Base' },
  { href: '/about/', label: 'About' },
];

function isActive(href: string): boolean {
  if (href === '/') {
    return currentPath === '/';
  }
  return currentPath.startsWith(href);
}
---

<header class="header">
  <a href="/" class="logo">Firewall Flamingos</a>
  <nav class="nav">
    {navLinks.map(link => (
      <a
        href={link.href}
        class:list={['nav-link', { active: isActive(link.href) }]}
      >
        {link.label}
      </a>
    ))}
  </nav>
</header>

<style>
  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 24px;
    border-bottom: 1px solid var(--border-subtle);
    position: relative;
    z-index: 10;
  }

  .logo {
    font-weight: 700;
    font-size: 16px;
    color: var(--text-primary);
  }

  .nav {
    display: flex;
    gap: 24px;
    font-size: 14px;
  }
</style>
